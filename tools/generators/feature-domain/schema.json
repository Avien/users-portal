{
  "cli": "nx",
  "id": "feature-domain",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Domain name in kebab-case (e.g. products, order-history)",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the domain name? (kebab-case)"
    }
  },
  "required": ["name"]
}